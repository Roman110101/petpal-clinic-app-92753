// Простой AI-ассистент без внешних API для стабильной работы

export class AIAssistant {
  constructor() {
    // Никаких внешних зависимостей
  }

  getResponse(userMessage: string): string {
    const message = userMessage.toLowerCase().trim();
    
    // Приветствие
    if (message.includes('привет') || message.includes('здравствуй') || message.includes('добр')) {
      return "Привет! 😊 Я Дарья, помощница ветеринарной клиники 'Море'! Чем могу помочь вашему питомцу?";
    }

    // Контакты
    if (message.includes('контакт') || message.includes('телефон') || message.includes('адрес') || message.includes('связать')) {
      return "📞 Наши контакты:\n• Москва: +7 (495) 234-56-78\n• Сочи: +7 (862) 234-56-78\n• Работаем круглосуточно 24/7\n• 4 филиала: Москва и 3 в Сочи";
    }

    // Время работы
    if (message.includes('время') || message.includes('работа') || message.includes('открыт') || message.includes('график')) {
      return "🕐 Мы работаем круглосуточно 24/7 без выходных! Экстренная помощь всегда доступна для ваших питомцев.";
    }

    // Запись на прием
    if (message.includes('запис') || message.includes('приём') || message.includes('прием') || message.includes('врач')) {
      return "📅 Для записи на приём:\n• Позвоните по телефону +7 (495) 234-56-78\n• Или используйте кнопку 'Запись' в приложении\n• Работаем круглосуточно!";
    }

    // Симптомы и болезни
    if (message.includes('симптом') || message.includes('болит') || message.includes('плохо') || message.includes('болезн')) {
      return "🏥 Понимаю ваше беспокойство! 😊 Для точной диагностики рекомендую:\n• Используйте кнопку 'Симптомы' для анализа\n• Или сразу запишитесь на приём к врачу\n• При экстренных случаях звоните немедленно!";
    }

    // Потеря аппетита
    if (message.includes('аппетит') || message.includes('ест') || message.includes('корм') || message.includes('пища')) {
      return "🍽️ Потеря аппетита может быть признаком разных проблем:\n• Стресс или смена обстановки\n• Проблемы с зубами или пищеварением\n• Инфекционные заболевания\n\nРекомендую показать питомца врачу для точного диагноза!";
    }

    // Вялость
    if (message.includes('вял') || message.includes('спит') || message.includes('актив') || message.includes('грустн')) {
      return "😔 Вялость - серьезный симптом:\n• Может указывать на болезнь\n• Проверьте температуру\n• Убедитесь, что пьет воду\n• Лучше всего - показать врачу!";
    }

    // Рвота/диарея
    if (message.includes('рвот') || message.includes('диаре') || message.includes('понос') || message.includes('жидк')) {
      return "⚠️ Рвота и диарея требуют внимания:\n• Может привести к обезвоживанию\n• Проверьте, что ел питомец\n• Обеспечьте доступ к воде\n• При ухудшении - срочно к врачу!";
    }

    // Кашель/чихание
    if (message.includes('кашл') || message.includes('чиха') || message.includes('насмор') || message.includes('дыхан')) {
      return "🤧 Проблемы с дыханием:\n• Может быть аллергия или инфекция\n• Проверьте температуру\n• Обеспечьте покой\n• При затрудненном дыхании - срочно к врачу!";
    }

    // Услуги
    if (message.includes('услуг') || message.includes('лечен') || message.includes('операц') || message.includes('вакцин')) {
      return "🩺 Наши услуги:\n• Терапия и диагностика\n• Хирургия и операции\n• Вакцинация\n• Груминг и стерилизация\n• Экстренная помощь 24/7";
    }

    // Цены
    if (message.includes('цена') || message.includes('стоимост') || message.includes('сколько') || message.includes('рубл')) {
      return "💰 Цены зависят от услуги:\n• Консультация: от 1500₽\n• Вакцинация: от 800₽\n• Точную стоимость уточните по телефону +7 (495) 234-56-78";
    }

    // Экстренная помощь
    if (message.includes('срочн') || message.includes('экстрен') || message.includes('скор') || message.includes('помощь')) {
      return "🚨 Экстренная помощь:\n• Звоните немедленно: +7 (495) 234-56-78\n• Работаем круглосуточно\n• Приезжаем на дом при необходимости\n• Не ждите - каждая минута важна!";
    }

    // Благодарность
    if (message.includes('спасиб') || message.includes('благодар') || message.includes('спас')) {
      return "😊 Пожалуйста! Рада была помочь! Если у вас еще есть вопросы о здоровье питомца - обращайтесь в любое время!";
    }

    // Прощание
    if (message.includes('пока') || message.includes('до свидан') || message.includes('прощай')) {
      return "👋 До свидания! Берегите своих питомцев! Если что-то случится - мы всегда на связи 24/7! 😊";
    }

    // Общие вопросы
    if (message.includes('что') || message.includes('как') || message.includes('почему') || message.includes('где')) {
      return "🤔 Хороший вопрос! Я помогу разобраться:\n• Расскажите подробнее о проблеме\n• Используйте кнопку 'Симптомы' для анализа\n• Или запишитесь на консультацию к врачу";
    }

    // По умолчанию
    return "😊 Понимаю! Я Дарья, помощница ветеринарной клиники 'Море'. Работаем круглосуточно и поможем с любой проблемой вашего питомца! Расскажите подробнее или используйте кнопки ниже для быстрой помощи.";
  }
}

export const aiAssistant = new AIAssistant();